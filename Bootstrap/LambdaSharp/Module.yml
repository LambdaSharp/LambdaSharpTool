Name: LambdaSharp

Version: 0.4

Description: LambdaSharp Environment

Exports:

  - Name: LambdaSharp-DeadLetterQueueUrl
    Description: LambdaSharp Dead Letter Queue (Url)
    Value: !Ref DeadLetterQueue

  - Name: LambdaSharp-DeadLetterQueueArn
    Description: LambdaSharp Dead Letter Queue (Arn)
    Value: !GetAtt DeadLetterQueue.Arn

  - Name: LambdaSharp-LoggingStreamArn
    Description: LambdaSharp Logging Stream
    Value: !GetAtt LoggingStream.Arn

Parameters:

  - Name: DeadLetterQueue
    Description: SQS queue used by LambdaSharp functions for failed messages
    Resource:
      Type: AWS::SQS::Queue
      Allow: None

  - Name: LoggingStream
    Description: Kinesis stream for consolidated logging
    Resource:
      Type: AWS::Kinesis::Stream
      Properties:
        RetentionPeriodHours: 24
        ShardCount: 1

